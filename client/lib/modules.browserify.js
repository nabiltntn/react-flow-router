/*
    Declare the set of required lib to import from npm packages to make them
    available in the client ( React libs for exemple).
*/
ImageLoader = require('react-imageloader')
ReactBootstrap = require('react-bootstrap/lib')
Spinner = require('react-spinkit')